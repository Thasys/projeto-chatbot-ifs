{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "dockerfile",
    "dockerfile": "Dockerfile"
  },
  "deploy": {
    "startCommand": "python -m streamlit run app_v2.py --server.port=$PORT --server.address=0.0.0.0 --server.headless=true"
  }
}
